import 'package:flutter/material.dart';

class KColor {
  // app color
  static const appColor = Color(0xFF48208E);
  static const btnColor = Color(0xFF48208E);

  static const filledColor = Color(0xFFEDEDED);
  static const subText = Color(0xFF747474);

// fee payment status
  static const success = Color(0xFF0C6500);
  static const pending = Color(0xFFFDD835);
  static const fail = Color(0xFFE53935);

  // common colors
  static const green = Color(0xFF0C6500);
  static const red = Color(0xFFE53935);
  static const black = Color(0xFF000000);
  static const white = Color(0xFFFFFFFF);
  static const blue = Color(0xFF0B5FE1);
}
